require 'spec_helper'
require_relative '../src/classes/passenger_train'
require_relative '../src/classes/passenger_wagon'
require_relative '../src/classes/cargo_wagon'

describe PassengerTrain do
  # subject { PassengerTrain.new('1') }
  # let!(:passenger_wagon) { PassengerWagon.new }
  # let!(:cargo_wagon) { CargoWagon.new }
  # describe '#hook_wagon' do
  #   context 'when wagon is cargo' do
  #     it 'not hooks it' do
  #       expect(subject.hook_wagon(cargo_wagon)).to eq(nil)
  #     end
  #   end

  #   context 'when wagon is passenger' do
  #     it 'hooks it' do
  #       expect(subject.hook_wagon(passenger_wagon)).to eq([passenger_wagon])
  #     end
  #   end
  # end
end